# 🚨 Don't change the code below 👇
two_digit_number = input("Type a two digit number: ")
# 🚨 Don't change the code above 👆
firtdigit=two_digit_number[0]
seconddigit=two_digit_number[1]
first=int(firtdigit)
second=int(seconddigit)
result=first + second 
print(result)
